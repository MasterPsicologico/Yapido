/**
 * ⚠️  DEPRECATED — migrado a `scripts/seed-lavadoras.ts` (TypeScript, lee .env.local).
 *  Este archivo se conserva en historial de git únicamente.
 *  En este commit se sobreescribe con un placeholder para evitar el uso accidental.
 */
throw new Error(
  'scripts/setup-firebase.js está deprecado. Usa `npm run seed:lavadoras` (npx tsx scripts/seed-lavadoras.ts).',
);
