/**
 * ⚠️  DEPRECATED — la lista de UIDs estaba hardcodeada.
 *  Usa `npx tsx scripts/setup-superadmin.ts <UID>` para añadir un superadmin.
 */
throw new Error(
  'scripts/init-superadmins.js está deprecado. Usa `npm run admin:add -- <UID>`.',
);
